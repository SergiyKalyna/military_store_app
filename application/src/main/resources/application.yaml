server.shutdown: graceful

spring:
    application:
        name: "military-store-app"
    jooq:
        sql-dialect: postgres
    docker:
        compose:
            enabled: false

springdoc:
    swagger-ui:
        path: '/swagger-ui.html'
        enabled: true

google:
    drive:
        credentials:
            path: "google-drive-credentials.json"
        folder:
            id: "1lEg2OeZmYbIJZoF0kp0azWnKs3034h9V"
